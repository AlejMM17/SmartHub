export const metadata = {
    title: "Actividades de Proyecto Profesores",
    description: "Professors projects page",
};

export default function ProfessorLayout({ children }) {
    return <>{children}</>
}
